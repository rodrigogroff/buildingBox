
- Register

	+ Meetings  (schedule) -> $ pushed to next billing

	+ Customization [/contract]

		(client delivers analysis over documentation)
			(total hours estimated by building box)
				(approved hours by client)
					(pending payment (pay first method))

						+ New project (maintenance) in client DevKit
							(execution)
							(working hours registered) 
							(bugs reported (re-work free))
							(homologation)
								(version accepted)
									(devkit upgraded)
									